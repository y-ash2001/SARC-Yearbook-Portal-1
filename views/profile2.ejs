<!DOCTYPE html>
<head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <title>Profile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <link rel="stylesheet" href="/assets/stylesheets/profile.css">
</head>

<body>
    <nav>
    <div class="hamburger">
        <div class="line"></div>
        <div class="line"></div>
        <div class="line"></div>
    </div>
    <ul class="nav-links">
        <li><a href="/<%= user._id %>/nominate">Nominate your Friend</a></li>
        <li><a href="/<%= user._id %>/nomination">Write a Caption!</a></li>
        <li><a href="#">Logout</a></li>
    </ul>
    
    </nav>
    <section class="landing">
            <div class="profile-photo">
                <img src="<%= user.imageUrl %>" alt="">
            </div>
            <div class="profile">
                <p class="name">Name: <%= user.name %></p>
                <p class="dept">Email ID: <%= user.email %></p>
                <% if(user.bitsId) { %> 
                    <p class="dept">BITS ID: <%= user.bitsId %></p>
                <% } %><
                p class="caption"><%= user.quote %></p>
                <form action="/addid/<%= user.id %>" method="POST">
                    Update BITS ID: <input type = "text" name="user[bitsid]">
                    <input type="submit" value="Submit">
                </form>
                <a href='/profile/<%= user._id %>/edit'><button type="button" class="btn">Edit Profile</button></a>
            </div>
    </section>
    <script src="assets/scipts/app.js"></script>
</body>
</html>